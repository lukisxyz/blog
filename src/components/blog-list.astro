---
import type { BlogPostType } from "../types";
import BlogCard from "./blog-card.astro";

type Props = Array<BlogPostType>;
const postLists = Astro.props;
---

<ul>
    {(postLists || []).map((d: BlogPostType) => <BlogCard {...d} />)}
</ul>
